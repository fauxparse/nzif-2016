%header{role: "banner"}
  .inner
    %h1= festival.name
    %h2= date_range(festival.start_date, festival.end_date)

%section
  .inner
    = link_to t('.register'), register_path(festival.year), class: "button"
